version: '3.3'

services:
  interview-predictor:
    build: .
    container_name: interview-predictor
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
