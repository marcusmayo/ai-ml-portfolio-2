# Interview Predictor - Local Deployment Requirements
# Tested and verified on Ubuntu 24.04 with Python 3.12

# ===== Web Server =====
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.0

# ===== Core ML Libraries =====
# CRITICAL: NumPy must be <2.0 for transformers compatibility
numpy>=1.26,<2.0
pandas==2.0.0
scikit-learn==1.3.0
scipy>=1.10.0

# ===== NLP & Transformers =====
transformers==4.44.2
safetensors==0.4.3
tokenizers==0.19.1
accelerate==0.20.0

# ===== PyTorch with CUDA 12.1 =====
# Install with: pip install --extra-index-url https://download.pytorch.org/whl/cu121 torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# ===== Audio Processing =====
faster-whisper==1.0.3
librosa>=0.10.0
soundfile==0.12.1

# ===== LLM API =====
google-generativeai==0.8.5

# ===== Utilities =====
tqdm==4.66.0
requests==2.31.0

# ===== Notes =====
# - Use install_local.sh for automated setup
# - PyTorch requires --extra-index-url for CUDA builds
# - NumPy <2.0 is critical for transformers compatibility
# - For CPU-only: pip install torch torchvision torchaudio (no extra index)
